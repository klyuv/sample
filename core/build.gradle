plugins {
    id 'com.android.library'
    id 'kotlin-android'
    id 'kotlin-kapt'
    id 'androidx.navigation.safeargs.kotlin'
    id 'kotlin-parcelize'
}

android {

    compileSdkVersion androidCompileSdkVersion
    buildToolsVersion androidBuildToolsVersion

    namespace "ru.klyuv.core"

    apply from: '../flavorsCommon.gradle'

    defaultConfig {
        minSdkVersion 26
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17.toString()
    }

    buildFeatures {
        viewBinding true
    }
}

dependencies {

    kotlinImplDependency.each { dep -> implementation dep.value }
    androidxAppCompatImplDependency.each { dep -> implementation dep.value }
    coreImplDependency.each { dep -> implementation dep.value }
    daggerImplDependency.each { dep -> implementation dep.value }
    glideImplDependency.each { dep -> implementation dep.value }
    customTabImplDependency.each { dep -> implementation dep.value }
    adapterDelegateImplDependency.each { dep -> implementation dep.value }
    daggerKaptDependency.each { dep -> kapt dep.value }
    presentationImplDependency.each { dep -> implementation dep.value }

}